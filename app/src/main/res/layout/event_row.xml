<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#455A64"
    android:gravity="center_vertical"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="2dp"
        android:background="#000"
        android:layout_marginBottom="2dp"
        >
        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="180dp"
            android:id="@+id/eventImageID"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            />
        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@id/eventImageID"
            android:layout_alignTop="@id/eventImageID"
            android:layout_alignRight="@id/eventImageID"
            android:layout_alignBottom="@id/eventImageID"
            android:layout_margin="1dp"
            android:textStyle="bold"
            android:textColor="#ffffff"

            android:shadowColor="#000000"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="1"

            android:fontFamily="sans-serif-condensed"
            android:textSize="30dp"
            android:textAllCaps="true"
            android:gravity="center"
            android:layout_below="@id/eventImageID"/>
        <TextView
            android:id="@+id/date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/title"
            android:drawableLeft="@drawable/ic_alarm_white_24dp"
            android:background="#80000000"
            android:textColor="#fff"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:layout_marginBottom="17dp"
            />

    </RelativeLayout>

</RelativeLayout>